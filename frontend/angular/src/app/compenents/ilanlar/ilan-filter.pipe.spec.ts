import { IlanFilterPipe } from './ilan-filter.pipe';

describe('IlanFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IlanFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
